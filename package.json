{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "Google API Url Shortener",
  "main": "main.js",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d js",
    "watch-sass": "sass --watch css/sass/style.scss:css/style.css",
    "watch-babel": "babel src --watch -d js"
  },
  "author": "Artur SD",
  "license": "ISC"
}
